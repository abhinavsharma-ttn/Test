pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Hello, World!'
                sh 'chmod +x abc.sh'
                sh '/var/lib/jenkins/workspace/task2/abc.sh'
            }
        }
    }
}
